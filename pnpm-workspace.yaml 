packages:
  # all packages in direct subdirs of apps/frontend/apps/
  - 'apps/frontend/apps/*'
  # all packages in subdirs of apps/frontend/package/
  - 'apps/frontend/package/*'
  # exclude packages that are inside test directories
  - '!**/test/**'
